# ============================================================================
# 環境變數範例檔案 (.env.example)
# ============================================================================
# 此檔案是個人化設定的範本，包含部署後的資源資訊。
# 
# 使用方式：
#   1. 複製此檔案為 .env：copy .env.example .env
#   2. 部署專案到你的 AWS 帳號：sam build && sam deploy
#   3. 從部署輸出取得資源資訊，填入 .env 檔案
#   4. .env 檔案已加入 .gitignore，不會提交到版本控制
# ============================================================================

# ----------------------------------------------------------------------------
# AWS 基本設定（可選）
# ----------------------------------------------------------------------------
# 注意：建議使用 `aws configure` 設定 AWS 認證，而非在此設定。
# 只有在特殊情況下（如 CI/CD）才需要在 .env 中設定。
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ----------------------------------------------------------------------------
# 部署後的資源資訊（必填，部署後填入）
# ----------------------------------------------------------------------------
# 這些資訊在部署後會因每個人的 AWS 帳號而不同，需要個人化設定。

# S3 Bucket 名稱（從 CloudFormation Outputs 取得）
# 格式：<stack-name>-bucket-<account-id>
S3_BUCKET_NAME=dropbex-mvp-bucket-202413277464

# SNS Topic ARN（從 CloudFormation Outputs 取得）
# 格式：arn:aws:sns:<region>:<account-id>:<topic-name>
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:202413277464:dropbex-mvp-topic

# API Gateway URL（從 CloudFormation Outputs 或 AWS Console 取得）
# 格式：https://<api-id>.execute-api.<region>.amazonaws.com/Prod
API_GATEWAY_URL=https://lryk1c1xjg.execute-api.us-east-1.amazonaws.com/Prod/

# ----------------------------------------------------------------------------
# 測試腳本設定（可選）
# ----------------------------------------------------------------------------
# 如果未設定，測試腳本會使用上面的 API_GATEWAY_URL 和 S3_BUCKET_NAME
TEST_API_URL=https://lryk1c1xjg.execute-api.us-east-1.amazonaws.com/Prod/
TEST_BUCKET_NAME=dropbex-mvp-bucket-202413277464

# ----------------------------------------------------------------------------
# 前端設定（可選）
# ----------------------------------------------------------------------------
# 如果使用前端功能，設定此變數後執行：.\build-frontend-config.ps1
FRONTEND_API_URL=https://lryk1c1xjg.execute-api.us-east-1.amazonaws.com/Prod/
