# ============================================
# AWS 基本設定
# ============================================
# AWS 區域（通常與 samconfig.toml 中的 region 相同）
AWS_REGION=us-east-1

# ============================================
# 部署後的資源資訊
# ============================================
# 這些資訊需要在首次部署後填入
# 可從 CloudFormation Outputs 或 AWS Console 取得

# S3 Bucket 名稱
S3_BUCKET_NAME=

# API Gateway URL
# 格式：https://<api-id>.execute-api.<region>.amazonaws.com/Prod
API_GATEWAY_URL=

# SNS Topic ARN（如果需要）
SNS_TOPIC_ARN=

# ============================================
# Cognito 設定（前端認證）
# ============================================
# 這些資訊需要在首次部署後填入
# 可從 CloudFormation Outputs 或 AWS Console 取得

# Cognito User Pool ID
# 格式：<region>_<pool-id>
COGNITO_USER_POOL_ID=

# Cognito App Client ID
COGNITO_APP_CLIENT_ID=

# Cognito Identity Pool ID
# 格式：<region>:<identity-pool-id>
COGNITO_IDENTITY_POOL_ID=

# ============================================
# 測試腳本設定
# ============================================
TEST_API_URL=
TEST_BUCKET_NAME=

# ============================================
# 前端設定
# ============================================
# 前端使用的 API Gateway URL（通常與 API_GATEWAY_URL 相同）
FRONTEND_API_URL=

# ============================================
# 其他設定（可選）
# ============================================
# 預簽名 URL 有效期（秒）
PRESIGNED_URL_EXPIRES_IN=3600

# Lambda 函數設定（可選）
LAMBDA_MEMORY_SIZE=256
LAMBDA_TIMEOUT=30